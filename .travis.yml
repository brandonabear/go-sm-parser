language: go
sudo: false
go:
  - tip
before_install:
  - go get github.com/mattn/goveralls
install:
  - go get golang.org/x/tools/cmd/cover
  - go get github.com/spf13/afero
script:
  - go test parser/* -v -covermode=count -coverprofile=coverage.out